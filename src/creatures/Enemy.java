package creatures;

public class Enemy extends Creature {
    public Enemy(String name, int level, int health, int toughness, int attack)
    {
        super(name, level, health, health, toughness, attack);
    }
}
